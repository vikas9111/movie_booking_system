<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.file_field :image %>

  <div>
    <%= form.label :title, style: "display: block", class: "form-label" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div>
    <%= form.label :genre, style: "display: block", class: "form-label mt-2" %>
    <%= form.text_field :genre, class: "form-control" %>
  </div>

  <div>
    <%= form.label :description, style: "display: block", class: "form-label mt-2" %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <div>
    <%= form.submit class: "btn btn-success mt-4" %>
  </div>
<% end %>
